<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodOrderingSystemYogiByte">
        <activity
            android:name=".FAQActivity"
            android:exported="false" />
        <!-- Splash Activity (LAUNCHER - shows first) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" /> <!-- Register Activity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" /> <!-- Home Activity -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" /> <!-- Find Restaurant Activity -->
        <activity
            android:name=".FindRestaurantActivity"
            android:exported="false" /> <!-- Order Details Activity -->
        <activity
            android:name=".OrderDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" /> <!-- My Orders Activity -->
        <activity
            android:name=".MyOrdersActivity"
            android:exported="false" /> <!-- Checkout Activity -->
        <activity
            android:name=".CheckoutActivity"
            android:exported="false"
            android:theme="@style/Theme.FoodOrderingSystemYogiByte" /> <!-- Order Food Activity -->
        <activity
            android:name=".OrderFoodActivity"
            android:exported="false" /> <!-- View Bookings Activity -->
        <activity
            android:name=".ViewBookingsActivity"
            android:exported="false" /> <!-- Book Seat Activity -->
        <activity
            android:name=".BookSeatActivity"
            android:exported="false" /> <!-- Restaurant Details Activity -->
        <activity
            android:name=".RestaurantDetailsActivity"
            android:exported="false" /> <!-- Offers Activity -->
        <activity
            android:name=".OffersActivity"
            android:exported="false"
            android:theme="@style/Theme.FoodOrderingSystemYogiByte" /> <!-- Order Activity - FIX THIS CLASS -->
        <activity
            android:name=".Order"
            android:exported="false"
            android:theme="@style/Theme.FoodOrderingSystemYogiByte" />
        <activity
            android:name=".SupportActivity"
            android:exported="false"
            android:theme="@style/Theme.FoodOrderingSystemYogiByte" />
    </application>

</manifest>